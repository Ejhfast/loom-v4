def bad(vm: Vm[Int]) with Vm
  case vm.drive()
  in Asked(q)
    case q.as_call(sys.io.Print)
    in Some(call) then vm.answer(call, 5)
    in None then ()
    end
  in Done(_) then ()
  in Fault(_) then ()
  end
end

1
