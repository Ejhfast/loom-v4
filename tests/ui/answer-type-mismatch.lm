def bad(vm: Vm[Int]) with Vm
  case vm.drive()
  in Asked(q)
    case q
    in Call(Io.Print, call, (_,)) then vm.answer(call, 5)
    in _ then ()
    end
  in Done(_) then ()
  in Fault(_) then ()
  end
end

1
