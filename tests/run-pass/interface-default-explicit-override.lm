interface Named
  def name(self): String
    "default"
  end
end

final class Item implements Named
  def name(self): String
    "explicit"
  end
end

def read[T: Named](value: T): String
  value.name()
end

read(Item())
