# The program entry: the trailing expression of `src/main.lm`.

use greeting
use mathlib.matrix

def run() with Io.Write
  m = matrix.Matrix(2, 3)
  hello = greeting.greet("Ada")
  println(hello).expect("the output writes")
  println(greeting.report(m)).expect("the output writes")
end

run()
