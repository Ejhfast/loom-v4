# Store closures in a list, select one, and call it.
base = 2
adders: [(Int) -> Int] = [
  { |x: Int|: Int x + 1 },
  { |x: Int|: Int x + base * 10 },
  { |x: Int|: Int x * 2 }
]

# Rebinding `base` after capture does not change the second closure.
base = 9

pick = adders.at(1)
pick(22)
