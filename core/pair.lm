# Core: Pair[A, B].
#
# A named two-value record. Use a tuple for a positional pair.

class Pair[A, B]
  first: A
  second: B

  def init(mut self, first: A, second: B)
    self.first = first
    self.second = second
  end

  def swap(self): Pair[B, A]
    Pair(self.second, self.first)
  end
end
